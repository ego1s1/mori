.gradle/
build/
local.properties
.idea/
.vscode/
*.iml
*.hprof
*.log
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class
.kotlin/
out/
# Third-party reference checkouts (e.g. Mihon) are inspiration only and must
# never be committed: they would bloat history and blur license boundaries.
# Take design ideas from them; always write our own implementations.
/reference/

# Local helper scripts (e.g. personal APK builders).
scripts/build-apk.sh

# Stray design-reference drops at the repo root (screenshots, mockups).
# Real assets live in module res/ dirs; these must never be committed.
/*.png
/*.jpg
/*.jpeg
