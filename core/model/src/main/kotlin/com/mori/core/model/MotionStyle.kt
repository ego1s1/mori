package com.mori.core.model

/** Motion personality for the UI. */
enum class MotionStyle {
    /** Spring physics, stagger, and shared-element transitions. */
    EXPRESSIVE,

    /** Short fades only; no bounce, stagger, or shared elements. */
    CALM,
}
